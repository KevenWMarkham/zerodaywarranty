.git
.venv
.github
.mypy_cache
.pytest_cache
.ruff_cache
__pycache__
**/__pycache__
*.pyc
tests
# Exclude the bulky doc sources but KEEP docs/design (served by the app, incl.
# the vendored three.js). Listed explicitly — the `docs/*` + `!docs/design`
# re-include trick is unreliable for nested files across Docker builders.
docs/zero-day-warranty
docs/reference
infra
backlog
catalog
service
templates
*.md
!README.md
